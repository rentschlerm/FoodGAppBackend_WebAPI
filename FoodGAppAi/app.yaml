runtime: python312
entrypoint: gunicorn -b :$PORT main:app

instance_class: F2

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 5

env_variables:
  FLASK_ENV: "production"
  SECRET_KEY: "AIzaSyCnrTpxE-n-Z1TJ2aWB7KUTTsUsa7Z4hGE"

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto
